import React from 'react';

import { Layout, Routes } from '..';

export const App = () => {
  return (
    <React.StrictMode>
      <Layout>
        <Routes />
      </Layout>
    </React.StrictMode>
  );
};
